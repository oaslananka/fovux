# Start the Server

Fovux Studio communicates with the backend via a local HTTP server.

Start it with:

```bash
fovux-mcp serve --http --tcp
```

Or click the **Start Server** command in the Fovux activity bar.

The server runs on `127.0.0.1:7823` by default and requires no network access.

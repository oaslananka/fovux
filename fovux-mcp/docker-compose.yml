services:
  fovux-mcp:
    build: .
    volumes:
      - ~/.fovux:/data/.fovux
    ports:
      - "7823:7823"
    environment:
      FOVUX_HOME: /data/.fovux
      FOVUX_LOG_LEVEL: INFO
    restart: unless-stopped
